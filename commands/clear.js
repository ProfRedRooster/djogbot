const discord = require("discord.js");

module.exports.run = async(bot, message, args) => {
message.reply("Het ping commando is uitgeschakeld wegens dingen");
}

module.exports.help = {

    name: "clear"

}
